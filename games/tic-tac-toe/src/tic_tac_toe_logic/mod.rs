//! The module for the tic-tac-toe logic. This contains all relevant implementations for
//! the networking system. These are implementations you can find in the backbone-lib in [`backbone_lib::traits`].

pub mod backend;
pub mod traits_implementation;
